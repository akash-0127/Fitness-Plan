{
  "name": "fitplan-nutrition-sync",
  "version": "1.0.0",
  "private": true,
  "description": "Scheduled sync of FitPlan nutrition + food database data from USDA FoodData Central + Open Food Facts",
  "scripts": {
    "sync": "node sync-nutrition-data.js",
    "sync:dry": "node sync-nutrition-data.js --dry-run",
    "sync:foods": "node sync-foods-data.js",
    "sync:foods:dry": "node sync-foods-data.js --dry-run"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  }
}
